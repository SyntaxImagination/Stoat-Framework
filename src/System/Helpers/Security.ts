/**
 * Module : Security Helper
 * Desc : Core Security features bundled by default 
*/

export const domainSecurity = async (request:Record<string, any>) => {
    
    return {
        status : 1
    }
}
